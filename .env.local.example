# Local Development Environment Variables
# Copy this file to .env.local and update with your local Supabase credentials

# Local Supabase Configuration (after running 'supabase start')
VITE_SUPABASE_URL=http://localhost:54321
VITE_SUPABASE_PUBLISHABLE_KEY=<get_from_supabase_status_command>
VITE_SUPABASE_PROJECT_ID=local

# To get your local credentials:
# 1. Run: supabase start
# 2. Run: supabase status
# 3. Copy the "anon key" value to VITE_SUPABASE_PUBLISHABLE_KEY above
# 4. Copy the "API URL" value to VITE_SUPABASE_URL above

# Note: Keep your .env file for production/remote Supabase credentials
# Use .env.local for local development (this file is gitignored)
