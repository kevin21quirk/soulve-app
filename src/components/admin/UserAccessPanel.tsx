import EnhancedUserAccessPanel from './EnhancedUserAccessPanel';

// Keep the original component name for backward compatibility
// but use the enhanced version internally
const UserAccessPanel = () => {
  return <EnhancedUserAccessPanel />;
};

export default UserAccessPanel;
